# Working-tree audit — 2026-09-17

This record covers checks run against the local working tree. `Passed` means the named executable check completed; it does not imply a browser flow was exercised.

| Area | Result | Evidence / limit |
| --- | --- | --- |
| Registration, login, refresh, logout, ownership | Passed | `scripts/verify_runtime.py` against a fresh disposable PostgreSQL database |
| Camera, zone, line, rule API and upload validation | Passed | Same live script; invalid video rejected |
| Camera creation form failure handling | Fixed and browser passed | Requires a local video and surfaces errors; upload failure rollback is code reviewed but was not triggered in the browser |
| Registration browser redirect | Fixed and browser passed | A 401 from `/auth/me` no longer redirects `/register` to `/login` while the user types |
| Dashboard header health label | Fixed | Removed an unconditional claim that inference was online |
| CPU detection, tracking, events, snapshots, analytics, WebSocket | Passed | Real YOLO model and 40-frame local video in `scripts/verify_runtime.py`; 38 frames processed, 2 dropped, 9 persisted events |
| Separate worker start, EOF, stop | Passed | `scripts/verify_worker_process.py` against the disposable database |
| Fresh migrations | Passed | `alembic upgrade head` on the disposable database |
| Backend unit/integration suite | Passed | `python -m pytest tests -q`: 107 passed |
| Frontend type/build | Passed | `npx tsc --noEmit`; `npm run build` |
| Docker Compose configuration | Passed | `docker compose config --quiet` |
| Browser registration, dashboard routes, camera upload and detail | Passed | Playwright/Chromium against local API and Next.js on a disposable database |
| Full Docker stack and remaining browser controls | Unverified | Image build was stopped while downloading a 554 MB PyTorch wheel; remaining controls were not exhaustively exercised |
| RTSP loopback, webcam, multi-camera runtime | Unverified | No live run in this pass |
| ONNX export/inference, benchmark, training/evaluation | Unverified | Unit tests passed for ONNX detector; no new execution or measurements in this pass |
| Hosted GitHub CI | Pending | Check the workflow result for the published commit |

The disposable database, users, uploaded test videos, and generated evidence from this pass were removed. Existing working-tree changes and older local artifacts were preserved. Broad Ruff checking currently reports 148 findings, mostly pre-existing FastAPI dependency-default rules and import/style findings; it is not a CI gate.
